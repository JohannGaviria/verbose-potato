[pytest]
pythonpath = .
asyncio_mode = auto
filterwarnings = ignore::DeprecationWarning:passlib.*
markers =
    db: tests that require a real PostgreSQL and/or Redis test instance (integration)
    e2e: tests that exercise the full HTTP application (end-to-end)
